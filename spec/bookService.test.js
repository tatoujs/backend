"use strict";

const bookService = require('../services/bookService');

describe('A book service', () => {

    it('should retrieve 20 books', () => {
        // bookService.getBooks()
        expect(true).toBe(true);
    });
});